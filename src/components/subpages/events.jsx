import { Button,Box,Typography ,Grid } from "@mui/material";
import mask from '../images/mask.jpg'
import { motion } from 'framer-motion'
function Events(){
    return(
        <>
         <Grid container sx={{justifyContent:'center',height:'100vh',alignItems:'center',backgroundImage:`url(${mask})`,backgroundSize:'cover'}}>
            <Grid item >
                
            </Grid>
         </Grid>
        </>
    );
}
export default Events;